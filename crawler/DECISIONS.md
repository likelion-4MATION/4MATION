# DECISIONS — 기술 선택·판단 로그

> 형식: 선택지 · 결정 · 근거. D2 핸드오프 보고서가 이 파일을 소스로 씀.

## D0 (금) — 수집 파이프

- **매니페스트 원천**: 02_검색범위정의서_초안.md vs 실물 kdic_필수페이지_URL매핑.csv(부재) → **02 정의서를 원천으로 필수 37건 확정**. 근거: csv 실물 없음이 사용자 확인됨, md가 단일 진실 원천.
- **kdic_필수페이지_URL매핑.csv 생성 방식**: 02 정의서 필수(●) 표 37행을 형식만 변환(사용자 승인). 컬럼 url·business_function·sub_category·page_type·coverage·variant. 근거: 규칙 7(임의 재구성 금지)의 승인된 예외. **sub_category·coverage·variant는 정의서에 없어 공란**(내용 창작 금지) — D1에서 브레드크럼/페이지 유형으로 채움. page_type엔 정의서 '페이지' 라벨을 그대로 보존.
- **build_manifest.py analysis_xlsx 선택 인자화**: 필수 positional → `nargs='?'`. 근거: 분석필요 xlsx 부재 시에도 필수 CSV 단독으로 매니페스트 확정 필요(사용자 지시 #3). 컬럼 자동 탐지·미리보기 로직은 그대로.
- **매니페스트 병합 결과 검수**: 컬럼 매핑 6/6 정확 일치, 37건 전량 source=필수, 링크만/제외/보류 유입 0건(xlsx 부재로 애초에 없음). 미리보기 눈 확인 완료.
- **robots 준수 방식**: urllib.robotparser가 KDIC robots.txt 읽기 실패(전 페이지 unreachable) + 실측 규칙이 Googlebot 전용이라 우리 UA엔 자동 미적용. → **config.POLICY_DISALLOW 경로 오버레이 신설**로 명시 차단. 근거: 절대규칙 1 준수는 robots 기술적 해석보다 우선. fins /cm/bbs/·www List.do/Dtl.do/srch를 실측 robots.txt대로 하드코딩. 차단 건은 수집 안 하고 report 기록만.
- **fins /cm/bbs/ 처리**: 매니페스트에 유지(화이트리스트, 규칙 6) + robots_blocked로 차단(규칙 1). 근거: 규칙 6(매니페스트=화이트리스트)과 규칙 1(disallow 수집 금지) 양립 — 목록엔 남기되 수집만 차단. 고객사 허용 시 즉시 해제 가능.
- **USER_AGENT contact**: TODO@example.com → hyunuk200202@gmail.com(현재 담당자). 근거: 팀 공식 주소 미확정. 확정 시 교체(주석 명시). KDIC 서버로 매 요청 전송되는 값이므로 공식 주소 권장.
- **재현성 판정 기준**: text_sha256 유지(raw는 CSRF/세션값 변동). 보호한도 1건 불일치는 **외부 사이트 비결정성(각주 금액 토큰 1억원↔5천만원)** 으로 판정 — 크롤러 결함 아님(HOLES H6). 크롤러 자체는 31/32 완전 재현.
- **캐노니컬 data/**: run1을 채택(보호한도=1억원 정상 변형). run2는 5천만원 스테일 변형 포함이라 미채택. run1/run2 디렉토리는 재현성 증거로 보존.
- **link_registry url 정책**: 02 정의서에서 확인되는 URL만 채우고 미확보분 공란 + HOLES 기록(사용자 지시 #5, 규칙 T3 창작 금지). 4건 채움/2건 공란. keywords는 함정 대비 각 5개(동의어 3+ 충족).

## D1 (토) — 선행 처리

- **robots_status 근본 수정(0-a)**: `urllib.robotparser.read()`(기본 Python-urllib UA로 GET → KDIC 차단이 D0 unreachable 원인)를 폐기하고, `requests.get(robots.txt, UA=config.USER_AGENT)` 후 `RobotFileParser.parse(lines)`로 변경. 결과: 두 호스트 robots.txt HTTP 200 정상 파싱(www 89B·fins 80B), 정상 페이지 robots_status가 `unreachable`→`allowed`로 정확화. **GET 실패 시 fail-open 금지** — 빈 규칙+`unreachable` 기록 유지, 실제 차단은 POLICY_DISALLOW가 담당. POLICY_DISALLOW 오버레이는 이중 안전장치로 유지(robots 규칙이 Googlebot 전용이라 우리 UA엔 can_fetch=allowed로 나오므로 오버레이 없으면 fins /cm/bbs/가 뚫림). **검증**: fins /cm/bbs/ 2건 disallowed, fins /ua/·www /sp/ allowed — 기대와 일치. 즉 근본 수정 후에도 fins /cm/bbs/ 5건은 여전히 차단.
- **H6 승격(0-b)**: D1 통과 기준에 "국내 보호한도 청크에 '5천만'을 **현행 한도로** 서술하는 청크 0건" 추가. grep은 검출 수단일 뿐, 검출건은 맥락 분류(이력 설명 '종전 5천만원→1억원 상향'=정상 / 현행 서술=실패) 후 판정. 파싱 전 canonical raw 재확인: `data/raw/...ProtSystProtLmts...` 는 5천만원×0·1억원×5 → 1억원 변형(정상). (분류 결과는 T3 청킹 후 기록)
- **분석필요 xlsx(0-c)**: 여전히 부재(폴더·~/Downloads·Spotlight 0건) → 병합·link_registry 공란 2건 보류 그대로. 테스트셋 원천은 fins FAQ(차단) 대신 **www FAQ + 질문형 헤딩 페이지**로 전환(H5).

## D1 (토) — 파서→벡터DB→검색

- **파서 컨테이너**: `.contents` 채택 (32/32 페이지 1개씩 존재 확인). `#container`는 브레드크럼·타이틀 포함이라 더 넓음. 노이즈 제거: `script/style/floatTop(글자크기·언어)/floatBottom(챗봇·상단이동)/btnBottomArea(조회·신청 바로가기)/form/input/button`. 근거: KDIC 텍스트 90%+가 GNB 노이즈, 컨테이너 협소화가 규칙기반의 핵심(03 문서).
- **표 처리**: 값 유실 없이 행 텍스트("cell | cell")로 직렬화, markdown 미화는 안 함(P2). `<table>` 보유 12/32. **T2 값 유실 스팟체크 5/5 통과**(3개월·5년·2개월·15억·5억원, 5개 페이지 교차). 표값 청크 반영도 확인(양도성예금증서·뮤추얼펀드·후순위채권 등).
- **브레드크럼**: `.location` 최상위 li 직계자식 첫 링크만 → 홈>대분류>중분류>현재 (드롭다운 형제메뉴 무시). D0 extract_breadcrumb(20개 과다추출) 대체. sub_category = 브레드크럼[1:] " > " 조인.
- **coverage**: 안내부={상속인 금융거래조회}(assignment 지정, btnBottomArea 조회 기능부 제거로 안내부만 남김), 그 외 전체. 부채증명원은 분석필요라 필수셋에 없음.
- **청킹**: CHUNK_SIZE=800 · OVERLAP=100 (03 문서 노션 권고 준용, 1설정 고정). 표 블록(연속 |라인) 중간분할 금지=단독 청크. FAQ 페이지는 질문-답변 1쌍=1청크('열기' 토글 제거). MIN_CHUNK=60(표 사이 낀 짧은 파편 인접 병합). **결과 134청크(문서 32) · 스키마 11필드 전건 유효**.
- **0-b '5천만' 맥락 분류(7건)**: 예금자보호제도 5건=FAQ 사례/예시금액(1억5천만원·계좌 예시 등, 현행 한도 서술 아님) · 착오송금 2건=착오송금 한도(별개 도메인, 예금보호한도 아님) · 미수령금 상속 1건=stale 소스(H6b). **국내 보호한도 도메인의 '5천만 현행 서술' 청크 0건 → 0-b 통과.** 보호한도 페이지 청크 재확인: 1억원×6 · 5천만원×0 · 퇴직연금/연금저축/사고보험금 별도한도 서술 존재.
- **임베딩 모델**: `jhgan/ko-sroberta-multitask`(로컬 한국어 SBERT, 768d) 선택. 선택지: 다국어 MiniLM(작지만 한국어 약함) · bge-m3(강하나 2GB↑ 무거움) · ko-sroberta(한국어 STS 튜닝, ~440MB, 균형). PoC 규모(134청크)에 균형점. 오프라인 실행. FAISS `IndexFlatIP` + 정규화 임베딩(=코사인). **적재 134 = 청크 134**.
  - **확정(팀 표준 없음 → 유지)**: 로컬 PoC용 선택. 모델명은 `rag.py`의 `MODEL_NAME` 상수 한 줄로 교체 가능. 프로덕션은 CLOVA 임베딩 검토(P4).
- **하이브리드(C)**: BM25(kiwipiepy 형태소 토큰, 명사/어간/영숫자 keep · rank_bm25 BM25Okapi) + dense, **RRF(k=60, pool=20)**. 평가 결과 **hybrid가 dense를 전 지표에서 상회**: hit@1 0.865>0.827 · hit@3 0.923>0.904 · MRR 0.903>0.867 (52문항). 리랭킹·파라미터 튜닝은 D1 금지(D2 판단).
- **아키텍처(A)**: `recollect(url)=fetch→parse→chunk→upsert(by doc_id)` 원자함수(pipeline.py). content_hash=가시텍스트 sha256(D0 재현성 지표와 동일) 불변 시 parse/chunk/embed 스킵. 검증: 1차 32 upsert(134청크)·5 차단, 2차 재실행 32 skip → 재수집 트리거 프로토타입 충족(03 §4). robots_blocked/error는 store에서 제외.
- **평가셋(B)**: 52건(대표6+FAQ28+헤딩18), 6대 업무 전부 커버, 정답=문서(parent_doc_id) 단위 gt_docs 리스트. **대표 6문항 top-3: hybrid 5/6·dense 6/6 → D1 통과(5+)**. 유일 hybrid 미적중="착오송금 어떤 경우 신청?"(hybrid rank8·dense rank2) — 같은 업무 유의사항/절차 페이지가 신청대상 페이지를 밀어냄. BM25가 doc-정밀 질의에서 역효과(리랭킹으로 D2에서 해소 검토). **오염체크 0건**(국내 보호한도 top-3에 해외 수치 없음).

## 07-13 (일) — HOLES 해소 세션 (0-c 병합 · H7/H8 정리)

- **xlsx 복원본 판정 분포(12행 — H1 경위 대조 자료)**: `분석필요태깅_포함여부분석.xlsx` Sheet1, 판정컬럼=예비판정. **포함 2**(no.1 예금자보호 FAQ · no.5 착오송금 신청대상) · **포함(안내부) 2**(no.4 상속인 금융거래조회 · no.9 부채증명원/금융거래정보신청) · **포함(D5대기) 1**(no.10 fins 채무정보조회 FAQ) · **링크만 5**(no.2 해외 보호한도 · no.3 지급대상 금융회사 검색 · no.6 신용회복 · no.7 파산면책 · no.8 개인회생) · **측정후결정 1**(no.11 불법행위신고 — 단 '최종 판단 근거' 텍스트는 "링크만…링크 전달"로 정제) · **제외 1**(no.12 부실책임조사 진행현황 조회). **주의: xlsx는 12행 — 02 정의서 집계 '분석필요 18건'·노션 초기 16건과 모두 불일치(H1 델타 대조 시 함께 확인 필요)**.
- **분석필요_예비판정.csv 부재(H3)**: 작업 폴더·~/Downloads 재탐색 0건 → 외부 교차 대조 불가, H3 잔존. 대체로 xlsx 내부 대조(예비판정 vs 최종 판단 근거) 수행 — 충돌성 항목 1건: no.11 예비판정 '측정후결정' vs 최종판단 '링크만'(병합 판단엔 무영향 — 두 판정 모두 비수집, link_registry 충전 근거로는 최종판단 채택). no.3도 다음액션("안내부 텍스트만 수집")과 최종판단("데이터 낡음 → 링크만")이 상충하나 예비판정=링크만 기준 비수집 유지.
- **build_manifest.py 병합 전 수정(자동매핑 검수 반영)**: ① 판정컬럼이 variant 힌트 '판'에 걸려 `variant←예비판정` 오매핑 → 판정컬럼을 필드 매핑에서 제외. ② `page_name`을 page_type 힌트에 추가(필수 CSV의 page_type=페이지 라벨 관행과 정합). ③ xlsx 업무명 번호 프리픽스('5. 채무조정 안내') 제거 — 기존 청크 business_function 표기와 통일. ④ 판정 '포함(안내부)' → coverage=안내부 전파(판정 텍스트 유래, 창작 아님). 근거: docstring의 "미리보기 눈 확인" 절차를 재현 가능하게 코드화.
- **병합 결과(0-c 완료)**: 37→**38건**(+부채증명원 1건, source=분석포함·coverage=안내부). 기존 37건 무변경(diff 확인). xlsx 포함 판정 중 3건(FAQ·상속인·착오송금 신청대상)은 필수와 URL 중복 → dedup(필수 우선). fins 채무 FAQ '포함(D5대기)'는 '대기' 필터로 병합 배제 — 이미 필수 매니페스트에 존재하며 **POLICY_DISALLOW 차단 유지(H5 허용 회신 전 수집 금지, robots_blocked 기록만)**.
- **link_registry 6건 전량 url 충전(H7 완결)**: ① illegal_act_report — 라이브 검증(200) 후 충전. 페이지 실체가 **예보 자체 '금융부실관련자 불법행위 신고센터'**(횡령·배임 신고 → 부실책임조사 → 수사의뢰, 02-758-0797)로 확인돼 기존 message의 "금감원 접수" 단정을 보정(부실관련자 불법행위=예보 소관 / 불법사금융·채권추심=금감원 병기). 판정 근거는 xlsx no.11 최종판단("재확인 후 링크 전달") — 측정후결정 A/B는 링크만으로 확정. ② overseas — xlsx no.2 '링크만' 판정 URL(KDIC 자체 해외 한도 비교표, 검증 200·'25.12 기준 한국 1억원)로 충전. **수집·청킹은 계속 제외**(타국 수치 오염 방지). FDIC/IADI 외부 직링크는 문서 미확보 → 창작 안 함. ③ 기존 4건(지급대행점·신용회복·파산면책·개인회생) — xlsx no.3/6/7/8 '링크만' 판정·소관 org 교차 일치 확인, 외부 직링크는 xlsx에도 없어 KDIC 안내 URL 유지. 지급대행점 조회 URL도 라이브 검증(200, 동적 검색 폼). 검증 3건 모두 저장 없음(수집 아님).
- **증분 recollect(38건)**: 31 skip · 2 upsert(부채증명원 신규 2청크 + 보호한도 재적재 2청크) · 5 robots_blocked(fins /cm/bbs/ 전부). **인덱스 136청크 · 문서 33 · faiss=emb=chunk_meta=chunks.jsonl 전부 136 일치 · 스키마 위반 0**. crawl_report.json 38건 기준 갱신(ok 33 / robots_blocked 5).
- **보호한도 upsert 원인 분석(H6 관측 추가)**: 07-13 저녁 fetch 텍스트가 **D0 캐노니컬 run1과 완전 동일**(9653자 · 5천만 0 · 1억 5)인데 스토어 해시와 불일치 → 재적재됨. 즉 오전(11:05) 인덱스 빌드 시점의 보호한도 텍스트가 다른 변형(H6 비결정성)이었다는 방증. recollect 트리거가 스테일을 자가 치유한 사례 — 현 인덱스는 정상 변형 확정.
- **pipeline.py 확장**: 라이브 recollect 실행 시 crawl_report.json을 run_crawl.py와 동일 스키마로 갱신(skipped/upserted→fetch 관점 ok 매핑). 근거: 매니페스트 38건 확장 후 리포트가 37건 기준으로 낡는 문제 — run_crawl 재실행(전량 이중 크롤) 없이 산출물 규약 유지.
- **crawler.py 레거시 정리(H8 완결)**: D0 best-effort extract_breadcrumb(GNB ~20개 과다 추출) 제거, parser의 D1 정제판(.location 직계 li 첫 링크만)을 crawler가 import해 단일 소스화 — data/meta breadcrumb도 정제판으로 기록됨(라이브 fetch 시 전 문서 meta 갱신 확인). tests/test_guards.py 픽스처를 실물 KDIC DOM(ol>li 직계+ulSelectBox 드롭다운)으로 교체, 드롭다운 형제 제외 검증 포함 8/8 통과.
- **신규 문서 검증(부채증명원)**: breadcrumb `['홈','제도·정책','지원자금관리','부채증명원/금융거래정보신청']` — 신규 유형(sprtfund 계열)에서도 .location 구조 유지(H8 재검증 통과). coverage=안내부 반영(parser COVERAGE_ANNAE 등록). 구비서류 표 값 전량 보존(공인인증서·휴대폰·위임장·인감증명서 등). **관찰 1**: 표 셀 내부 `<br>` 다항목이 비-파이프 라인을 생성해 표 원자성 휴리스틱(연속 |라인)이 끊김 → 긴 표가 행 경계에서 청크 분할될 수 있음(값 유실 없음·오버랩 유지, HOLES H13 신설·P2 표→markdown 파싱 근거 사례). **관찰 2**: 페이지 원문의 교부방법/유의사항 문단이 '경력증명서/확인서'를 언급(부채증명원 문맥과 불일치 — KDIC 원문 자체 오기 추정), 무수정 원칙에 따라 보존.
- **0-b 확장 재검증('5천만' 전수, 136청크)**: 검출 청크 6 · 발생 8회 — ① 예금자보호 FAQ 4청크(#03/#09/#11/#14) = 사례·예시금액(1억5천만원 적립금, 3/4/5천만원 계좌 예시 등)이며 현행 한도는 일관되게 1억원 병기 → 정상. ② 상속인 조회 1청크 = H6b 기지 stale(미수령금 도메인, 제외 대상). ③ 착오송금 반환지원절차 1청크 = H9 기지(별개 도메인 한도, 예솜이 웹툰 인용 stale). **국내 보호한도 도메인 '5천만 현행 서술' 0건 → 통과.** 신규 stale 발견 0(부채증명원 검출 없음), 보호한도 페이지 청크 1억원×6·5천만×0. 소스 무수정 유지. (D1 '7건'과의 수치 차이는 셈법(발생 vs 청크) 차이 — 이번 기록 기준: 청크 6·발생 8)
- **평가 재실행(52문항 유지)**: dense 3지표 D1 대비 ±0(.827/.904/.867), hybrid hit@1 .865·hit@3 .923 ±0, MRR만 .903→.900(−0.003 — 신규 2청크 BM25 idf 편입에 따른 미세 변동, 회귀 아님). 대표 6문항 문항별 랭크까지 D1과 동일(hybrid 5/6·dense 6/6, 유일 미적중=H10 기지). 오염체크 0건 — 해외 페이지는 미수집(링크만)이라 구조적으로 혼입 불가. **평가 문항 확장은 안 함** — 부채증명원 문항 갭(커버리지 1문서)만 eval_report에 기록, 팀 확장 시 2~3문항 권장. eval_report.md에 D1 델타 병기(수기 주석 — eval.py 재실행 시 재생성되므로 본 항목이 원본 보존).

## 07-22 (수) — 테스트셋 400건 확장 + 임베딩 모델 교체(D2 판단 사항 실행)

- **테스트셋 300→400건 확장 + gt_docs 라벨 QA**: 6대 업무 실제 결손 기준으로 100건 신규 추가(임의 비율 아님). 과거 `difflib.SequenceMatcher` ratio≥0.4 자동완성 34건 및 그에 따른 hit@3 .795→.812 비교는 **철회**한다. 문자열 유사도는 relevance 동치를 증명하지 않으므로 자동으로 `gt_docs`를 변경하지 않는다. 후보는 `generate_near_duplicate_candidates.py`가 `review_status=pending`으로만 생성하며, 전수 사람 검토와 근거 기록 후에만 별도 수동 변경한다.
- **RRF 1차 재튜닝(ko-sroberta 기준)**: 캐시 기반 그리드서치(dense/sparse 후보 1회 계산 후 융합 수식만 재계산)로 `rrf_k` 60→10, `pool` 20→30. hit@3 .812→.830. D1에서 "리랭킹·파라미터 튜닝은 D1 범위 밖(D2 판단)"으로 유보했던 항목 실행.
- **cross-encoder 재랭킹 검증 후 미채택**: `BAAI/bge-reranker-base`(정확하나 쌍당 1.1초, 실사용 부적합) / `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1`(경량, 쌍당 170ms)로 400건 전수 재검증 — 소표본(40문항) 예비 테스트에선 개선처럼 보였으나 전수 재검증시 순이익 없음(hit@3/MRR 오히려 소폭 하락). `rag.py`에 `hybrid_rerank` 모드로 옵트인 코드는 남기되 기본값 미채택.
- **business_function 메타데이터 부스팅 검증 후 미채택**: corpus centroid 기반 경량 분류기(쿼리 임베딩-업무영역 centroid 코사인 유사도)로 하드필터(SelfQueryRetriever 방식)·소프트부스트(BM25F 필드가중치 방식) 둘 다 400건 전수 검증. 분류기 정확도 58.5%로 하드필터는 hit@3 최대 −4.3%p, 소프트부스트도 hit@1/MRR 미세 상승 대신 hit@3 소폭 하락 — net 이득 없음. 원인 조사 중 `kdic-fins-cm-bbs-selectFaqNramtAply`(38청크)가 business_function="고객 미수령금 신청"으로 오태깅(실제 내용은 예금보험금 지급절차)돼 centroid를 오염시킨 것을 발견, 라벨만 재태깅(청크 재청킹은 불필요 — 이미 Q&A 단위로 적절히 분할돼 있었음). 검색 스코어링 자체엔 무영향(hit@3 불변), 오류분석 리포트의 "타업무혼입" 오탐만 제거(37→31건).
- **임베딩 모델 교체: `jhgan/ko-sroberta-multitask`(768d) → `BAAI/bge-m3`(1024d)**. D1에서 "PoC 규모(134청크)에 균형점"으로 bge-m3(당시 "강하나 2GB↑ 무거움"으로 보류)를 제외했던 결정을 D2 시점(215청크·400문항 규모, 오프라인 실행 유지)에 재검토 — 경량 모델의 한계가 명확해진 뒤 재평가. bge-m3 dense 단독조차 기존 hybrid를 상회(dense hit@3 .665→.848). RRF 2차 그리드서치(60개 조합)로 `rrf_k=5, dw=2.0, sw=1.0` 채택(dense가 sparse보다 강해져 가중치 상향). **hit@1 .562→.672 · hit@3 .830→.930 · MRR .706→.800 · 미적중 68→28/400 · 업무간 혼입 31→5건**. 그리드서치 예측치와 실제 `eval_TF.py` 전수 재실행 결과 일치 확인(캐시 근사 유효성 검증). 기존 ko-sroberta 인덱스는 `data/index_backup_ko_sroberta/`에 롤백용 보관(git 비대상).
- **비용/트레이드오프**: bge-m3가 ko-sroberta보다 훨씬 무거움(다운로드 ~2.2GB, 215청크 재임베딩 ~200초 — 1회성 인덱스 빌드 비용). 오프라인 로컬 실행 기조는 유지(D1 원칙 그대로).
- **page_title 충돌 발견 및 해소(착오송금인용/수취인용 "유의사항")**: 착오송금 반환신청 근사문서 통합 조사 중, 별개의 실존 문서 2건(`MsdrprAttnMttr`=착오송금인·`AddrseAttnMttr`=수취인)이 동일 page_title "유의사항"을 써서 기존 `chunk_embed_text()`(title+본문만 사용)로는 임베딩 입력이 구분 불가함을 발견. 두 문서를 인위 통합하는 안은 규칙 7(임의 재구성 금지) 위반이라 기각, 대신 이미 파서가 브레드크럼에서 추출해둔 `sub_category`(예: "착오송금반환지원 > 착오송금인 > 유의사항")를 임베딩 입력에 포함하는 방식으로 해소 — 신규 필드 생성 없음.
  - **1차 시도(전 청크 일괄 적용) 후 폐기**: `chunk_embed_text()`가 title 대신 sub_category를 전 청크(215개)에 일괄 프리픽스. 400건 재평가 결과 hit@3만 소폭 개선(.930→.932)했지만 hit@1 .672→.650·MRR .800→.787로 순손실 — FAQ류 문서(다수 청크가 동일 sub_category 공유, 예: `SprtFndCncmDclrFaq` 12청크 전부 "제도·정책 > 지원자금관리 > 부실책임조사 > 금융부실관련자 은닉재산신고 > FAQ" 공유)에서 상위 브레드크럼 반복이 임베딩을 희석시켜 은닉재산 신고 hit@1 .548→.371(최대 타격)까지 하락. business_function 부스팅 실패(D2 상단)와 같은 "메타데이터 일괄 적용이 국소 이득·전역 손실"패턴 재확인.
  - **2차: 충돌 문서만 좁혀 적용(채택)**: `_colliding_titles()`로 서로 다른 parent_doc_id가 같은 page_title을 쓰는 케이스만 걸러냄 — 38문서 중 실제 충돌은 2쌍뿐(**"유의사항"**=착오송금인/수취인, **"개요"**=예금자보호제도 selectProtSystProtSumr/selectProtSystProtTrgtPrdctSumr). 해당 청크만 `disambiguate=True`로 sub_category 프리픽스 적용(dense 재임베딩은 `Store.build_and_save()`에서 저장 직전 일괄 재계산 — upsert는 doc_id 단위라 다른 문서의 존재를 알 수 없어서), BM25 코퍼스와 reranker 페어링도 동일 청크만 반영. **결과(전체 vs D2 베이스라인): hit@1 .672→.688 · hit@3 .930→.932 · MRR .800→.810 · 미적중 28→27/400 — 전 지표 순개선, 회귀 없음.** 예금자보호제도 hit@1 .712→.788(+7.6%p, "개요" 충돌 해소 효과 유력) · 착오송금 반환 신청 hit@1 .542→.569 동반 개선.
  - **커버리지 갭**: 400건 테스트셋에 "송금인/수취인" 키워드 질의가 1건뿐이고 그마저 gt_docs가 두 유의사항 문서를 겨냥하지 않아, 이번 fix가 겨냥한 시나리오(착오송금인용 vs 수취인용 유의사항 구분)를 직접 검증하는 문항이 없음 — 팀 확장 시 2문항(예: "착오송금인이 지켜야 할 유의사항은?" / "착오송금 수취인 유의사항은?") 추가 권장(D1 §07-13 부채증명원 갭 기록과 동일 패턴).
- **착오송금 반환 신청 미적중률 정밀분석(18.1%, 타 도메인 1.5~9.2% 대비 이상치) — 원인 3갈래 확정**: 미적중 13건 전부 ❌동일업무오답(타업무 오염 아님), 순수 도메인 내부 세분화 문제로 확정.
  1. **www↔fins 병렬 섹션 실질 콘텐츠 중복**: `kmrsItrdProc`(www, "착오송금반환지원 절차")와 `MtrsGvbkSprtProc`(fins, "반환지원절차") 본문이 5단계 절차 설명 문장 단위로 거의 동일(청크 단위 최량매칭 difflib ratio=0.728) — "유의사항" 제목충돌과 같은 www/fins 병렬 게재 구조이나 이번엔 제목이 아니라 본문 자체가 근사 중복이라 sub_category 프리픽스로는 미해소.
  2. **문서 길이/도메인어 밀도 불균형**: `kmrsItrdAplyMthd`(신청방법, 250자 단문)가 정답인 질문 5건이 반복 미적중 — 더 길고 "착오송금" 반복이 많은 인접 문서(FAQ 12청크·반환지원절차 4청크 등)에 dense·BM25 양쪽에서 밀림.
  3. **송금인/수취인 party 혼동 잔존**: `MsdrprPossDcmntGudn`(송금인 구비서류)·`MsdrAddrsePossDcmntGudn`(수취인 구비서류)는 title이 달라(collision 아님) 07-22 1건 fix 범위 밖인데도 "이의제기" 질의에서 party가 뒤바뀌는 사례 2건(#9,#13 in error_analysis.md) 잔존.
  - **옵션 1(in-domain contrastive 파인튜닝)·옵션 4(BM25 길이정규화 국소튜닝)는 미승인 — 보류.**
  - **옵션 2(근사정답 상호등록) — 적용 후 철회**: 최초엔 difflib.SequenceMatcher 청크 단위 최량매칭으로 착오송금 12문서 조합 중 후보 2쌍(①`kmrsItrdProc`↔`MtrsGvbkSprtProc` ②`MsdrprPossDcmntGudn`↔`MsdrAddrsePossDcmntGudn`)을 수동 대조해 `data/testset_natural_400_v3.jsonl` 4문항에 `kmrsItrdProc`를 직접 추가했으나, 문자열 유사도만으로 gt_docs를 자동 변경하지 않는다는 정책이 확립되며 새 워크플로(`generate_near_duplicate_candidates.py`, review_status=pending 후보만 생성) 밖이라 `git checkout`으로 되돌림(재추가하지 않음). 두 후보쌍 모두 `data/near_duplicate_candidates.jsonl`에 이미 포함돼 pending 상태(①4건·②45건) — 상세 수치·원문대조 근거는 그쪽 파일이 정본, 여기서는 중복 서술하지 않음.
  - **주의(재현 사고) — 유실 확인됨**: 07-22 오후 `testset_natural_400_v3.jsonl`에서 위 4문항 외에도 채무조정·미수령금 등 도메인 전반의 gt_docs가 광범위하게 축소된 상태가 발견됨. **사용자가 세션 내에서 직접 확인**: 이 축소는 사용자 본인이 직접 수행한 의도적 gt_docs 정밀화 작업이었음. 이후 위 정책 확립 과정에서 `git checkout`으로 커밋(`ba4ec78`) 상태로 되돌리면서 **이 수동 정밀화 작업도 함께 유실된 것으로 확인됨** — 재작업 필요 시 `generate_near_duplicate_candidates.py` pending 후보를 사람이 재검토하는 새 워크플로로 다시 수행해야 함(과거 방식 재사용 불가).
- **착오송금 hit@3 정체(.819, 타 도메인 .908~.985 대비 유일하게 0.9 미만) — "hub 문서" 진단 및 부분 해소**: 위 3원인을 재정밀화 — 미적중 13건의 top-3 오답 슬롯(최대 39개) 중 26개(67%)가 단 4개 문서(`MtrsGvbkSprtProc` 9회·`kmrsItrdAplyTrgt` 8회·`selectFaqMsdrGvbkAply` 5회·`MsdrprPossDcmntGudn` 4회)에 집중됨을 확인. 이 4개 문서는 청크가 많거나(20·12·4개, cf. 정답 문서 대부분 1개) 청크당 텍스트가 길어(최대 800자) 질의 의도(방법/서류/이의제기 등)와 무관하게 반복적으로 후보 풀을 점유 — 고차원 최근접이웃 검색의 "hubness" 현상(Radovanović·Nanopoulos·Ivanović, *Hubs in Space*, JMLR 2010)과 일치하는 패턴. 해결 옵션 4종(S1 BM25 길이정규화·S2 저밀도 청크 보강·S3 hub 청크 검색제외·S4 파인튜닝) 중 **S1만 승인**.
  - **S1 채택: BM25 `b` 0.75→0.85**. dense 결과 캐시(쿼리 임베딩 400건 1회 계산) 후 b값만 바꿔 BM25 재적합하는 그리드서치(0.00~1.00, 0.05~0.25 간격)로 확인 — b=0.85가 기본값(0.75) 대비 **전체·착오송금 6개 지표(hit@1·hit@3·MRR × 2그룹) 중 5개 개선·1개(착오송금 hit@3) 동률로 유일하게 열세 없음**. `rag.py`에 `BM25_B=0.85` 상수 추가. **canonical 재검증(`eval_TF.py`) 결과: hit@1 .688→.693 · hit@3 .932→.935 · MRR .810→.813 (전체) / hit@1 .569→.583 · hit@3 .819→.819(동률) · MRR .714→.721(착오송금)** — 회귀 없음, 타 업무도 예금보험금 hit@3 .908→.923 등 동반 개선.
  - **한계**: b 튜닝은 hub 억제를 부분적으로만 완화 — 착오송금 hit@3는 여전히 .819로 0.9 미달. hub 문서 자체의 구조적 쏠림(청크 수·길이 불균형)을 없애려면 S2(저밀도 청크 보강)·S3(hub 청크 검색제외) 또는 S4(파인튜닝)가 추가로 필요 — 미승인 상태로 보류 중.

## 07-23 (목) — S1 이후 후속 튜닝: rrf_k 재탐색(기각) · CHUNK_SIZE 500 채택 · 다중 granularity 병합(대기) · tabArea 파서 수정(기각)

- **rrf_k 단독 재그리드서치(3~200) — 채택 후보 없음**: `grid_rrf_k.py` 신설, dense/sparse 후보 캐시 후 rrf_k만 재계산(400건 전수, 6개 업무 개별 확인). `rrf_k≤3`에서만 착오송금 hit@3 .819→.833(+0.014)로 개선되나 예금보험금 안내 hit@3가 동시에 −0.031~−0.046 하락(rrf_k=1일 땐 은닉재산도 −0.065) — "특정 업무 큰 하락 금지" 원칙 위반으로 기각. `rrf_k=15`는 어느 업무도 하락시키지 않고 예금보험금 +0.015하나, 정작 착오송금은 hit@3 불변(.819)·MRR −0.039 악화라 목표에 무의미. **결론: rrf_k=5(현행) 유지, 코드 변경 없음** — RRF 가중치만으로는 착오송금 문제가 해소되지 않음을 확인(원인이 융합 가중치가 아니라 문서/청크 구조임을 재확인). 로그: `data/rrf_k_grid_log.txt`.
- **CHUNK_SIZE 그리드서치(400/500/600/800) — 500 채택**: `experiment_chunk_size.py` 신설(격리 인덱스 `data/index_backup_chunk<size>/`에 빌드, 프로덕션 미접촉 상태로 우선 검증). 사전 확인(하위 하단 스크립트) — 착오송금 오염 상위 4개 hub 문서 중 CHUNK_SIZE에 실제로 민감한 것은 `MtrsGvbkSprtProc`(4청크·최대800자) 정도뿐이고 `selectFaqMsdrGvbkAply`(FAQ, split_faq() 경로라 무관)·`MsdrprPossDcmntGudn`(표/소단락 위주라 800 근처도 아님)은 구조적으로 영향권 밖 — 이 실험이 hub 4종 중 최대 1.5개만 겨냥 가능하다는 한계를 인지하고 진행. 결과(800 대조군이 프로덕션 실측과 완전 일치해 하네스 검증 완료): `500`이 착오송금 hit@3 .819→.847(+.028)·예금보험금 안내 .923→.954(+.031)를 개선하면서 은닉재산 −.032·고객미수령금 −.015 소폭 하락(400은 은닉재산 −.065로 500보다 열세, 600은 개선폭이 작음) — 전체 micro hit@3 .935→.938. **CHUNK_SIZE 500이 4개 후보 중 유일하게 순이득이 손실보다 뚜렷이 크다고 판단해 채택**(은닉재산 95.2%는 여전히 상위권). 근거: 청크 세분화가 hub 문서의 과도한 포괄적 매칭을 완화한다는 passage-retrieval 문헌(Callan, *Passage-Level Evidence in Document Retrieval*, SIGIR 1994; Dai & Callan, SIGIR 2019)과 정합. 로그: `data/chunk_size_grid_log.txt`.
  - **프로덕션 반영**: `chunk.py CHUNK_SIZE 800→500`(OVERLAP=100 불변) 적용 → `pipeline.py --use-cache --rebuild`로 오프라인 전량 재빌드(249청크) → `eval_TF.py` canonical 재실행 결과가 격리실험과 완전 일치(hit@1 .690·hit@3 .938·MRR .810 전체 / 착오송금 hit@3 .847·MRR .726) 확인. **롤백**: `data/index_backup_chunk800/`(BM25_b=0.85·bge-m3 기준 CHUNK_SIZE=800 인덱스 전체 보존)로 언제든 되돌릴 수 있음.
- **다중 청크 크기(granularity) RRF 병합 — 검증 완료, 프로덕션 미반영(대기)**: `experiment_multi_granularity.py` 신설. 이미 빌드된 400/500/600/800 인덱스를 재임베딩 없이 재사용, 크기별 hybrid(dense+sparse RRF) 결과를 문서 단위로 다시 RRF 융합(2단계 융합, 가중치 전부 1.0 — 튜닝 없이 "병합 자체 효과"만 우선 검증). 15개 조합 중 `400+500+800`이 전체 hit@1 .705(전 조합 최고, 단일 800 대비 +.012)·MRR .821(전 조합 최고)를 기록하며 착오송금(.847)·예금보험금(.954) 개선을 유지한 채 500 단독의 부작용(은닉재산 −.032→−.016, 고객미수령금 −.015→±0)을 절반 이상 상쇄. 근거: RRF(Cormack, Clarke & Buettcher, SIGIR 2009)의 "임의 개수 순위리스트 융합" 일반성 재적용(신규 이론 아님), LlamaIndex `AutoMergingRetriever`/LangChain `ParentDocumentRetriever` 등 공식 OSS의 multi-granularity 인덱싱과 사상은 유사하나 구현은 이번에 직접 설계. **한계**: 15개 조합을 held-out 없이 동일 400건에 그리드서치해 다중비교 과적합 위험 있음(이 프로젝트엔 애초 held-out셋이 없어 D1/D2 전체 튜닝이 같은 한계 공유). **비용**: 인덱스 N개 유지·재수집 시 N배 반영·질의당 N배 조회가 필요해 CHUNK_SIZE 단일 변경보다 아키텍처 부담이 큼 — **사용자에게 500-단독 vs 병합-포함 여부를 물었으나 다음 턴에 항목2·3(파서/스키마 개선) 우선 진행으로 방향이 정해져 이 옵션은 미착수 상태로 남음**(다음 실험 후보 1순위). 로그: `data/multi_granularity_grid_log.txt`.
- **tabArea 형제탭 노이즈 제거 시도 — 진단 유효하나 net 손실로 롤백**: 착오송금인/수취인 party 혼동(`MsdrprPossDcmntGudn`↔`MsdrAddrsePossDcmntGudn`, `data/near_duplicate_candidates.jsonl` 45건 pending) 원인 조사 중, 두 문서의 raw HTML `.contents` 내부에 실제 `<div class="tabArea"><ul><li>착오송금인</li><li>착오송금수취인</li></ul>...</div>` 형제탭 위젯이 있고, 이 `<ul>`(상대 탭 라벨까지 포함)이 노이즈 필터를 거치지 않고 본문 청크 맨 앞줄에 그대로 섞여 들어가는 것을 확인(전 38건 중 `.contents` 내 실제 `.tabArea` DOM이 있는 문서는 이 2건뿐 — grep의 다른 매치는 `<script>`류 참조일 뿐 실제 콘텐츠 아님, `BeautifulSoup.select()`로 확인). `parser.py NOISE_SELECTORS`에 `.tabArea > ul` 추가(형제 탭의 실제 콘텐츠 div는 `<ul>`의 형제 요소라 유실되지 않음, 구조 확인 완료) 후 오프라인 전량 재빌드+재평가한 결과, **착오송금 hit@3 .847→.833(−.014)로 오히려 악화**(신규 미적중 2건 vs 신규 개선 1건, net −1). 근본 진단(형제탭 라벨이 party 구분 신호를 오염시킨다는 구조적 근거)은 여전히 유효하나, 제거된 텍스트가 해당 두 문서 한정으로도 BM25 코퍼스 전역 idf 통계에 영향을 줘 무관한 질의들까지 흔든 것으로 추정 — **롤백**(`parser.py`에 시도·기각 근거를 주석으로 남기고 `NOISE_SELECTORS`는 원상복구, `pipeline.py --use-cache --rebuild`로 재빌드해 CHUNK_SIZE=500-단독 상태로 복원 확인, `eval_TF.py` 수치가 CHUNK_SIZE 실험값과 완전 일치).
- **www/fins 근사중복(`kmrsItrdProc`↔`MtrsGvbkSprtProc`) pending 4건 — 원문 직접 대조로 독립 검증(사용자 승인 대기, gt_docs 미변경)**: `near_duplicate_candidates.jsonl`의 자동 유사도 점수(ratio=0.7338 3건·0.1755 2건)를 신뢰하지 않고 두 문서 전체 청크 원문을 직접 대조. `kmrsItrdProc`(www, 2청크, 5단계 공식 절차 요약)와 `MtrsGvbkSprtProc`(fins, 6청크, 동일 5단계 요약 뒤에 "예튼이·예솜이" 삽화 스토리 콘텐츠가 추가로 이어짐)는 **첫 청크(공식 절차 5단계)만 사실상 동일**하고 이후 콘텐츠는 다름 — 파서 결함이 아니라 KDIC 사이트가 실제로 같은 절차를 공식 요약판(www)과 삽화 해설판(fins) 두 형태로 게재한 것으로 확인. pending 4건 중 일반적 "잘못 보낸 돈 돌려받을 수 있어요?"류 3건은 두 문서 모두 실제로 정답을 담고 있어 `kmrsItrdProc`를 `MtrsGvbkSprtProc`의 gt_docs에 병기할 근거가 있다고 판단되나, "송금 버튼 누르기 전에 뭘 확인해야 하나요?"(사전주의사항) 1건은 그 답이 `MtrsGvbkSprtProc`의 삽화 스토리 뒷부분에만 있고 `kmrsItrdProc`엔 없어 **병기 근거 없음**(자동 점수만으로는 구분 불가한 거짓양성 사례). gt_docs 변경은 정책상 사람 승인 없이 자동 반영하지 않으므로 사용자 확인 대기 중.
