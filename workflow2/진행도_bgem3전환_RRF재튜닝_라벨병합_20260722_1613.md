# 진행도 — bge-m3 전환 · RRF 재튜닝 · 팀원 라벨/재태깅 병합 (2026-07-22 16:13)

> 이어서: `진행도_소프트부스트채택_테스트셋400_20260722_1226.md` 이후.
> 핵심 지표 기준: **eval_TF.py (ANALYZE_MODE=hybrid_bf)**. 평가셋 `testset_natural_400.jsonl`(400).
> 한 줄 요약: 임베딩을 ko-sroberta→**bge-m3**로 교체하고, 강한 dense에 맞춰 검색을 재튜닝
> (가중 RRF α0.6 · 부스트 0.005 · **RRF_K 60→5**). 팀원 라벨 교정·재태깅 병합. hit@3 0.855→**0.935**.

---

## 1. 임베딩 bge-m3 전환
- `MODEL_NAME = BAAI/bge-m3` (1024d). `EMB_DIM 768→1024`, `build_and_save`가 실제 임베딩
  차원으로 인덱스 생성(모델 교체 내구성). 재빌드 시 dim assert 에러 해소.
- 효과(단독): 미적중 58→45, **불명 버킷 26→13**(미분류 질의도 dense가 잘 잡음 → 일반화 개선).

## 2. 검색 재튜닝 (bge-m3 기준, 과보정 제거)
강한 dense 위에서 약한 모델용 기본값이 과보정으로 작동 → dense 1위 정답을 파이프라인이
끌어내리는 현상 확인(hybrid_bf hit@1 < dense hit@1). 스크래치 A/B로 재튜닝:

- **가중 RRF**: `RRF_ALPHA = 0.6` (dense 0.6 / sparse 0.4). `hybrid_soft`에 반영.
- **부스트 완화**: `BF_BOOST 0.02 → 0.005` (오분류 정답 재매장 방지).
- **RRF 상수**: `RRF_K 60 → 5` (rrf_k 스윕서 단조 개선, 최적).

### rrf_k 스윕 결과 (400셋, bge-m3)
| 구성 | rrf_k | hit@1 | hit@3 | hit@5 | MRR |
|---|---|---|---|---|---|
| hybrid_bf(현행이전) | 60 | 0.642 | 0.910 | 0.955 | 0.773 |
| **hybrid_bf(채택)** | **5** | **0.657** | **0.935** | 0.958 | **0.791** |
| hybrid_nobf(순수) | 5 | 0.635 | 0.858 | 0.917 | 0.753 |

- **bf 장치는 유효**: 같은 rrf_k=5에서 hybrid_bf 0.935 vs 순수 hybrid 0.858 → **+7.7pp**.
  약한 모델용으로 만든 소프트부스트+cap+재태깅이 bge-m3 위에서도 값을 함. 유지 확정.

## 3. 팀원(타 브랜치) 라벨/재태깅 병합
같은 베이스에서 갈라진 팀원 브랜치의 교정을 원문 대조 검증 후 반영.

- **테스트셋 gt 교정 8건**(`testset_natural_400.jsonl`):
  - 교체 2: 정산금 인터넷신청(상속인조회→신청방법), 포상금 지급주체(신고센터→FAQ 2건).
    원 라벨이 질문에 답 못 하는 문서였음(원문 확인).
  - 추가 6: 정산금/지급대행점(3개 통일)/Fast-Track 등 두 문서 중복 내용 이중라벨.
- **selectFaqNramtAply 재태깅**(`chunks.jsonl`·`chunk_meta.jsonl` 38청크):
  우리 키워드 규칙이 미수령금4·예보6으로 오태깅 → 팀원 수동검증 매핑(공통2/예보4/예금보험금32)
  으로 교체. **미수령금 오태깅 0** (블랙홀 오염 근원 제거). #15(잔액조회서)는 테스트셋 라벨과
  자기모순이던 것도 해소. `chunk.py`에 인덱스 오버라이드(`FAQ_BF_OVERRIDE`) 추가 → 재빌드 내구성.

## 4. MMR·팀원 0.930 규명 (오해 정리)
- 팀원 "MMR로 95%"의 실제 지렛대는 **MMR이 아니라 RRF(k=5)**(요약 헤더로 확인). MMR은 지난번
  기각 + 이번에도 원인 아님 → 종결.
- 팀원 업로드 error_analysis(300셋, 미적중 76, selectFaqNramtAply 60회 혼입)는 **재태깅 이전
  구버전**이라 0.930 결과와 별개. 교차 브랜치 숫자 직접 비교는 테스트셋/코퍼스 차이로 부정확 →
  자기일관된 우리 내부 결과로 판단.
- 결론: **우리 hybrid_bf + rrf_k5 = 0.935 > 팀원 0.930** (데이터 개선 얹힌 효과).

## 5. 현재 확정 파이프라인
**hybrid_bf = 업무 소프트부스트(α0.6·β0.005) + doc-cap(1) · RRF_K=5 · bge-m3 · 400셋.**
- hit@1 0.657 / hit@3 0.935 / hit@5 0.958 / MRR 0.791 · 대표6 6/6.
- 여정(hit@3): ko 0.855 → bge-m3 0.887 → +재튜닝(α/β) 0.902 → +gt/재태깅·rrf_k5 **0.935**.

## 6. 다음
1. 리랭킹(보류)은 남은 정분류(업무내 형제문서, 착오송금 최약)의 주 해결책 — CLOVA 키 수령 시 재개.
2. rrf_k=5는 다소 공격적 → 실트래픽 강건성 모니터링(대안 k=10은 hit@5 최고·근소차).
3. eval_report/error_analysis는 rrf_k5 반영 후 재생성 필요(현 커밋 시점 기준 재실행).

## 산출물/커밋
- 채택: `rag.py`(bge-m3·EMB_DIM·RRF_ALPHA·BF_BOOST·RRF_K·hybrid_soft), `chunk.py`(오버라이드),
  `testset_natural_400.jsonl`(gt 8건), `chunks.jsonl`(재태깅), eval·eval_TF(헤더 표기).
- 실험: `eval_softboost.py`·`eval_rrf_tune.py`·`eval_rrfk.py`·`eval_mmr.py`(재현판).
- 워크플로우: 본 문서.
